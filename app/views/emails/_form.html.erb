<%= form_with(model: email, local: true) do |form| %>
  <% if email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(email.errors.count, "error") %> prohibited this email from being saved:</h2>

      <ul>
      <% email.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email_template_id %><br>
    <%= form.select :email_template_id, EmailTemplate.where(:user_id => current_user.id).collect { |f| [f.subject, f.id] } %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
